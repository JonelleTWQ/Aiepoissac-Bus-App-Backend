https://www.youtube.com/watch?v=pkg0J6lpKT4&list=PL_c9BZzLwBRKWfmG3DTZ49tDV6Mn7GEQ2
^ node.js course

mkdir = make directory
cd = change directory
.md = markdown

All things git:
.gitignore is used to tell git what files to not put into Version Control (?) not sure how to use it
git add . = "Track all the changes I made, including new files or edits."
git commit -m "blablabla" = "Save my changes and add this description."
git push origin main = "Upload my stuff to GitHub, on the 'main' branch."


IMPT!!
In MongoDB Atlas, rmb to change network access!!


For personal copying:
node yay.js

git add .
git commit -m "(message here)"
git push origin main
